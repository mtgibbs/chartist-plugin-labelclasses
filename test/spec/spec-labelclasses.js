describe('ctLabelClasses', function () {
  'use strict';

  beforeEach(function () {

  });

  afterEach(function () {

  });

  it('should be defined in chartist', function () {
    expect(window.Chartist.plugins.ctLabelClasses).toBeDefined();
  });
});
